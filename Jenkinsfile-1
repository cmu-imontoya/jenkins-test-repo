pipeline {
    agent any
    tools {
        maven 'maven'
    }

    stages {
        stage('Git Checkout') {
            steps {
                checkout scmGit(branches: [[name: '*/main']], extensions: [], userRemoteConfigs: [[url: 'https://github.com/cmu-imontoya/jenkins-test-repo']])
                echo 'Git Checkout Completed'
            }
        }

        stage('SonarQube Analysis') {
            steps {
                withSonarQubeEnv(installationName: 'sonar') {
                    sh 'mvn clean verify sonar:sonar -Dsonar.projectKey=jenkins -Dsonar.host.url=http://34.138.255.10:9000'
                    echo 'SonarQube Analysis Completed'
                    echo 'SonarQube Analysis Completed'
                }
            }
        }
    }
}
